git clone <your-repo-url>
cd ai-toolkit

# install deps, then edit config/sdxl_lora_cli.example.yaml or config/anima_lora_cli.example.yaml
# dataset/model paths can be absolute, or repo-relative paths resolved from the config file

# SDXL
python train_sdxl_lora.py --config-file config/sdxl_lora_cli.example.yaml

# Anima
# train_anima.py auto-detects vendor/sd-scripts, so sd_scripts_root can usually be omitted
python train_anima.py --config-file config/anima_lora_cli.example.yaml
